let bubbles = [];
let bubbleSpeed = 1; // 초당 이동 거리 (기본값 1)

let bubbleImg1;
let bubbleImg2;
let bubbleGif;
let bubbleTitleGif;
let pressStartGif;

let pointerImage = false;

function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(255, 255, 255);
}
